const Navbar = () => {
  return (
    <nav>
      <div>
        <a href="#home" className="flex items-center gap-2 ">
          <p> Shots Coffee </p>
        </a>

        <ul>{}</ul>
      </div>
    </nav>
  );
};
